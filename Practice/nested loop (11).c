#include <stdio.h>

int main () {
    int i,x,y,j;
    scanf("%d%d",&x);
    for(i=1; i<=x; ++i) {
            for(j=1;j<=y; ++j){
                x=j*j;
            }

    }
    printf("the ans is %d\n",j);

return 0;
}

