#include <stdio.h>

int main ()
{
   int i,x,sum=0,num[5];
   printf("Enter five numbers \n");

   for(i=0; i<5; ++i)
   {
       scanf("%d",&num[x]);
       sum = sum + num[x];
   }
   printf("ans is %d\n",sum);

}
